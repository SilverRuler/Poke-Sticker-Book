import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.silverruler.pokestickerbook',
  appName: 'pokemon-collector',
  webDir: 'dist'
};

export default config;
